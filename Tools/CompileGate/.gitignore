# Build output of the gate: compiled dependency DLLs and the response files that
# produced them. Regenerated by run.sh on demand, ~3 MB per configuration, and
# specific to the editor version and reference set it was last run against —
# committing it means a stale binary shadowing whatever the next run should build.
.deps*/
